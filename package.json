{
  "name": "leftovers-again",
  "version": "0.6.4",
  "description": "Pokemon Showdown - Bot Client",
  "main": "lib/main.js",
  "files": [
    "lib",
    "templates",
    "CONTRIBUTING.md",
    "README.md",
    "FAQ.md"
  ],
  "scripts": {
    "build": "babel src -d lib && babel scripts -d lib/scripts && cp src/data lib -R && ./build/copy-static-files.sh",
    "build:windows": "babel src -d lib && babel scripts -d lib/scripts && cp src/data lib -R",
    "clean": "rm -rf lib",
    "docs": "./node_modules/.bin/esdoc -c esdoc.json",
    "docs:publish": "curl --data \"gitUrl=git@github.com:dramamine/leftovers-again.git\" https://doc.esdoc.org/api/create",
    "docs:update": "npm run docs:publish",
    "lint": "./node_modules/eslint/bin/eslint.js src",
    "test": "jasmine",
    "test:coverage": "NODE_PATH='src:bots:spec' istanbul cover  -x '**/spec/**' jasmine",
    "develop": "NODE_PATH='src' nodemon --watch src --exec babel-node src/start.js",
    "start": "npm run clean && npm run build && node lib/start.js",
    "start:quick": "node lib/start.js",
    "server": "cd deps/Pokemon-Showdown && node app.js",
    "client": "./scripts/open-client.sh",
    "generate": "./lib/scripts/generate.js",
    "release": "npm run clean && npm run build && npm run test:coverage && npm run docs && npm run docs:publish && echo 'Great! Now run npm version patch (or whatever) and npm publish'"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dramamine/leftovers-again.git"
  },
  "bugs": "https://github.com/dramamine/leftovers-again/issues",
  "keywords": [
    "pokemon"
  ],
  "author": "marten",
  "license": "BSD-2-Clause",
  "dependencies": {
    "cheerio": "^0.20.0",
    "glob": "^7.0.3",
    "handlebars": "^4.0.5",
    "minimist": "~1.2.0",
    "inquirer": "^1.0.2",
    "lodash": "^4.6.1",
    "ws": "^1.0.1"
  },
  "devDependencies": {
    "babel": "^6.5.2",
    "babel-cli": "^6.8.0",
    "babel-eslint": "^5.0.0",
    "babel-plugin-module-alias": "^1.4.0",
    "babel-resolve-relative-module": "^1.0.1",
    "esdoc": "^0.4.6",
    "eslint": "^2.4.0",
    "eslint-config-airbnb": "^6.1.0",
    "eslint-plugin-react": "^4.3.0",
    "jasmine-es6": "^0.1.10"
  },
  "engine": "node >= 4.2.1"
}
